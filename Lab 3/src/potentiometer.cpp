/*#include <Arduino.h>

//Put your potentiometer pin assignment here
//Put potentionmeter in pin 0 of ESP32

#define POTENTIOMETER A1  //do not use a semicolon when defining!

const int sensorPin = 0;

void setup() {
  Serial.begin(115200);//match this baud rate
}

void loop() {
  //This line should print out the values from the sensor by Serial.println(??);
  int sensorValue = analogRead(POTENTIOMETER);
  Serial.println(sensorValue);
  delay(50);
}*/

